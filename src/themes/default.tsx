import { Theme } from './themeTypes';

const defaultTheme: Theme = {
  colors: {
    primary: '#000',
    themeColor: '#990000',
  },
  fonts: {
    text: '"BrandFont", sans-serif',
  },
};

export default defaultTheme;
