import React from 'react';
import Dummy from '../components/atoms/Dummy';

const PageTemplate = ({ globalConfig, data }) => (
  <Dummy globalConfig={globalConfig} data={data} />
);

export default PageTemplate;
